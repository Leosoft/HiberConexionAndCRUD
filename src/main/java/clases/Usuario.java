/*
import jakarta.persistence.*;

@Entity
@Table(name = "registro") // Especifica el nombre de la tabla en la base de datos
public class Usuario {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @Column(name = "id") // Especifica el nombre de la columna en la tabla
    private int id;

    @Column(name = "nombre") // Especifica el nombre de la columna en la tabla
    private String nombre;

    @Column(name = "apellido") // Especifica el nombre de la columna en la tabla
    private String apellido;

    @Column(name = "email") // Especifica el nombre de la columna en la tabla
    private String email;

    // Otros campos

    // Getters y setters
}
*/